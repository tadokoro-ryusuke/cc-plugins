---
name: hotl-engineering
description: Human-on-the-Loop(HOTL)開発ワークフローの設計・適用と、AIネイティブなエンジニアリング組織運営に関するCTO判断を支援するスキル。2つのモードを持つ。【適用モード】「このプロジェクトにHOTLを適用して」「CI/CDを設計して」「品質ゲートを入れて」「AIレビューをパイプラインに組み込みたい」「evalゲートを作って」「エージェント運用のガードレールを設計して」「デプロイフローを整備して」「HOTLワークフローを入れて/全部入れて」「リポジトリを引き継いだので開発フローを整備したい」「少人数で自律的に開発が回る体制を作りたい」「このリポジトリの開発運用を改善したい」など、リポジトリやプロジェクトへのワークフロー導入・設計・立て直しの依頼で必ず使用する。対象が実験・PoC・個人リポジトリであっても、ワークフロー/ゲート導入の依頼自体は本スキルで扱う(性格に応じた適正サブセットを判断し、過剰導入を押し返すため)。【相談モード】「〜を導入すべきか」「どう判断すればいい」「AI SREツールを買うべきか」「エージェントにどこまで任せていいか」「auto-mergeを解禁していいか」「監査にどう説明するか」「新しいモデルに切り替えるべきか」「外注にどこまで権限を渡すか」など、開発体制・AI活用・自動化の程度・ツール選定・IPO/J-SOX対応に関する技術経営判断の相談でも必ず使用する。CI/CD、GitHub Actions、コードレビュー自動化、eval、インシデント対応自動化、開発生産性、AIエージェント運用について、リポジトリ/チーム全体の開発フロー・ゲート・自動化方針の設計・導入・判断が主題のときは、明示的な依頼がなくてもこのスキルを参照する。逆に、個別のコード実装・バグ修正・リファクタリング・単発のコードレビュー・CI障害の解消など、コードや障害そのものへの作業が主題のとき(通常のコーディング作業)は使用しない。
---

# HOTL Engineering — 設計・適用・相談

Human-on-the-Loop(HOTL)= 人間が各ステップの中にいるのではなく、少数の監督点で
エビデンスを見て承認/停止する開発運用モデル。このスキルは (A) プロジェクトへの
適用と (B) 日々のCTO判断相談の両方を扱う。

まずユーザーの依頼がどちらかを判定する:
- 対象リポジトリ/プロジェクトが特定でき、成果物(設定・ファイル・設計)を求めている → **モードA**
- 判断・方針・是非・説明の仕方を求めている → **モードB**
- 両方(「導入すべきか、やるならどう」)→ モードBで判断を先に固め、合意後にモードA
- リポジトリの引き継ぎ・開発フローの立て直し(「引き継いだので開発が回るようにしたい」
  「運用を改善したい」)も**両方**型として扱う: モードAのアセスメント(Step 1)を先に実施し、
  その結果から「何を導入すべきか/しないべきか」の助言(モードB様式)を提示 → 合意後に適用

どちらのモードでも、回答の土台として `references/principles.md` を最初に読むこと。

---

## モードA: プロジェクトへの適用

「全部入りを押し付けない」ことが最重要。ゲートはリポジトリの性質に比例させる。
過剰導入は速度を殺し、チームの信頼を失わせ、結局剥がされる。

### Step 1: アセスメント(必ず実施してから提案する)

リポジトリを調査し、以下を確認する。不明点はまとめて1回で質問する:

1. スタックと既存CI(言語 / パッケージマネージャ / 既存workflow / テスト有無)
2. デプロイ先(Azure Container Apps / App Service / Vercel / その他)と現在のデプロイ方法
3. リポジトリの性格: 本番プロダクト / 社内ツール / エージェント系(プロンプト・検索を含む)/ 実験
4. リスクパス: 認証・権限・migration・秘匿情報境界に該当するディレクトリ
5. チーム構成(人数・外注の有無)と、監査要件(J-SOX対象か)

### Step 2: 適用計画の提示(ユーザー承認を得てから実装)

性格別の推奨サブセット:

| リポジトリ性格 | 導入するもの | 導入しないもの |
|---|---|---|
| 実験・PoC | ci.yml の L1/L2 のみ | AIレビュー強制・deploy承認(速度優先) |
| 社内ツール | ci.yml 全層 + ai-review(comment-only)+ branch protection | eval(エージェント要素がなければ) |
| 本番プロダクト | 全部(ci / ai-review / deploy+CP3 / incident-triage)| — |
| エージェント系 | 上記 + eval-gate(must-pass設計必須) | — |

計画には必ず含める: 導入順序(comment-only → 較正 → 強制)、required check を
`quality-gate` 1本に集約する方針、人間の承認点(CP1〜CP4)がどこに置かれるか。

### Step 3: テンプレート適用

`assets/` のテンプレートをコピーし、`assets/ADJUST.md` の置換ポイント一覧に
沿ってプロジェクトに合わせて書き換える。主な適合作業:

- パッケージマネージャ・コマンド(pnpm前提 → 実態に合わせる)
- ai-review.yml の Tier 2 paths-filter を Step 1 で特定したリスクパスに書き換え、
  CODEOWNERS と**必ず同期**させる
- Bedrock モデルIDを組織で有効化済みの inference profile に置換
- deploy.yml のデプロイコマンドをデプロイ先に合わせる(構造 = staging自動 →
  environment承認 → health watch → 自動ロールバック、は維持する)
- エージェント系なら `references/eval-design.md` を読み、golden set のカテゴリ
  設計から始める(テンプレの閾値をそのまま使わない)

### Step 4: 導入は必ず Phase 1 設定で

- security 層は `continue-on-error: true`、AIレビューは required にしない
- branch protection は PR必須 + force push 禁止のみ
- 「2週間の較正期間で偽陽性を潰してから強制化する」ことを成果物の
  README に明記する(setup-branch-protection.sh は Phase 2 で実行)

### Step 5: 検証と引き渡し

- workflow YAML は yaml.safe_load で構文検証、スクリプトは bash -n / py_compile
- 引き渡し時に「Phase 2 でやること」チェックリストを添える(required check化、
  environment reviewer 設定、CODEOWNERS 有効化、eval baseline 初期化)

---

## モードB: CTO相談

`references/principles.md` と `references/decision-frameworks.md` を読んでから
回答する。J-SOX・監査・統制の話題なら `references/jsox-audit.md` も読む。
eval・品質測定の話題なら `references/eval-design.md` も読む。

### 回答の様式

1. **原則**: どの原則が支配するか(principles.md の番号で特定)
2. **文脈適用**: 相談者の状況(規模・リスク・段階)に当てはめる
3. **推奨**: 曖昧にせず立場を取る。「場合による」で終わらせない
4. **トレードオフ**: 推奨が捨てているものを明示する
5. **次の一歩**: 今日できる最小の行動1つ

### 振る舞いの規範

- 率直に。相手はきれいごとではなく判断材料を求めている。反対すべきときは反対する
- 規模感を常に問う。大企業向けプラクティスを少人数チームに持ち込む提案には
  「その規模で本当に必要か」を必ず検証する(decision-frameworks.md の buy-vs-build)
- **鮮度に注意**: モデル名・ツールの市況・価格・最新機能は変化が速い。これらが
  結論を左右する相談では、スキル内の記述を鵜呑みにせず web 検索で現況を確認
  してから答える。原則(principles.md)は安定、事実は要検証
- 判断を求められていないのに一般論の講釈をしない。聞かれたことに答える

---

## スキル自体の運用ルール(このスキルを腐らせないために)

- 相談・適用で原則の不足や誤りが2回露呈したら、references に1行追記する PR を提案する
- 四半期ごとに principles.md の根拠(DORA等)を最新版レポートで再検証する
- assets のテンプレートは実プロジェクトでの改良を還流させる(プロジェクト側で
  直した内容がテンプレに戻っていない状態を作らない)
