---
allowed-tools: Bash(pnpm:*), Bash(npm:*), Bash(git:*), Read, Write, Edit, Grep, Glob, Task(subagent_type:dev-core:tdd-practitioner)
description: "単独のTDDサイクルを実行します。Red→Green→Refactor→Commitの完全サイクル"
argument-hint: "[機能名/テスト名] [--red テストのみ] [--green 実装のみ]"
---

# TDD サイクル実行

t-wada 式の TDD サイクルを単独で実行します。

## 実行フロー

### Phase 1: Red 🔴（失敗するテストを書く）

```
Task(subagent_type: "dev-core:tdd-practitioner")
prompt: |
  以下の機能に対する失敗するテストを作成してください。

  【機能】
  $ARGUMENTS

  【制約】
  - テストは具体的で明確であること
  - 1 つの機能に対して 1 つのテスト
  - 境界値とエッジケースを考慮
```

テストを実行して失敗を確認：

```bash
pnpm test --watch=false 2>&1
```

### Phase 2: Green 🟢（テストをパスさせる）

最小限のコードでテストをパス：

```bash
pnpm test --watch=false 2>&1
```

### Phase 3: Refactor 🔨（リファクタリング）

テストがグリーンの状態を維持しながらコード改善：

- 重複の排除
- 命名の改善
- 構造の整理

### Phase 4: Commit ✅（コミット）

```bash
git add .
git commit -m "feat: $FEATURE_NAME - TDD cycle complete"
```

## 出力形式

```
【TDD サイクル】

🔴 Phase 1: Red
   📝 テスト作成: src/features/auth/login.test.ts
   ❌ テスト失敗: 期待通り

🟢 Phase 2: Green
   📝 実装: src/features/auth/login.ts
   ✅ テスト成功: 1/1

🔨 Phase 3: Refactor
   📝 リファクタリング: 重複コード排除
   ✅ テスト成功: 1/1

✅ Phase 4: Commit
   📝 コミット: feat: Add login feature - TDD cycle complete

【サイクル完了】
   所要時間: 15 分
   テスト追加: 1
   コード行数: +45
```

## オプション

- `--red`: Phase 1 のみ実行（テスト作成）
- `--green`: Phase 2 のみ実行（実装）
- `--no-commit`: コミットをスキップ

## 使用例

```bash
# 完全サイクル
/dev-core:tdd "ユーザーログイン機能"

# テストのみ作成
/dev-core:tdd "パスワードリセット" --red

# 実装のみ
/dev-core:tdd --green
```
