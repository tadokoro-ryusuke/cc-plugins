---
allowed-tools: Bash(pnpm:*), Bash(npm:*), Bash(node:*), Read, Write, Grep, Glob
description: "Eval駆動開発を実行します。pass@kメトリクスで品質を測定し、回帰テストと能力テストを管理します"
argument-hint: "[--create 新規Eval作成] [--run Eval実行] [--report レポート生成]"
---

# Eval 駆動開発

コードの品質を定量的に測定し、継続的な改善を追跡します。

## Eval とは

- **能力テスト**: 新機能が期待通り動作するか
- **回帰テスト**: 既存機能が壊れていないか
- **pass@k メトリクス**: k 回の試行で成功する確率

## コマンド

### Eval の作成

`--create` オプションで新しい Eval を作成：

```bash
/dev-core:eval --create "ユーザー認証機能"
```

Eval 定義ファイルを生成：

```yaml
# evals/auth-login.yaml
name: ユーザー認証機能
description: ログイン機能が正しく動作することを検証
type: capability

tests:
  - name: 正常ログイン
    input: { email: "test@example.com", password: "valid" }
    expected: { success: true }

  - name: 無効なパスワード
    input: { email: "test@example.com", password: "invalid" }
    expected: { success: false, error: "INVALID_CREDENTIALS" }

pass_criteria:
  pass_at_1: 0.9 # 1回目で90%成功
  pass_at_3: 0.95 # 3回以内で95%成功
```

### Eval の実行

`--run` オプションで Eval を実行：

```bash
/dev-core:eval --run evals/auth-login.yaml
```

### レポート生成

`--report` オプションでレポートを生成：

```bash
/dev-core:eval --report
```

## 出力形式

```
【Eval 実行結果】

📊 Eval: ユーザー認証機能
   実行回数: 3
   成功率: 100% (3/3)

   テスト結果:
   ✅ 正常ログイン: pass@1 = 1.0
   ✅ 無効なパスワード: pass@1 = 1.0

📈 メトリクス:
   pass@1: 1.0 (目標: 0.9) ✅
   pass@3: 1.0 (目標: 0.95) ✅

📋 履歴:
   2024-01-20: pass@1 = 0.8 → 1.0 (+25%)
```

## ベストプラクティス

1. **新機能追加時**: 能力テストの Eval を作成
2. **バグ修正時**: 回帰テストの Eval を作成
3. **リファクタリング時**: 既存 Eval が pass することを確認
4. **CI/CD 統合**: すべての Eval が pass することを要求

## ディレクトリ構造

```
evals/
├── capability/     # 能力テスト
│   ├── auth-login.yaml
│   └── user-crud.yaml
├── regression/     # 回帰テスト
│   └── issue-123-fix.yaml
└── reports/        # 実行レポート
    └── 2024-01-20.json
```

## 使用例

```bash
# 新しい Eval を作成
/dev-core:eval --create "API レスポンス検証"

# 特定の Eval を実行
/dev-core:eval --run evals/capability/api-response.yaml

# すべての Eval を実行
/dev-core:eval --run-all

# レポートを生成
/dev-core:eval --report
```
