---
allowed-tools: Bash(git:*), Bash(pnpm:*), Bash(npm:*), Read, Write, Grep
description: "進捗スナップショットを作成・比較します。テスト状態、カバレッジ、変更ファイルを記録し、進捗を追跡します"
argument-hint: "[--create 作成] [--compare 比較] [--list 一覧] [--restore 復元]"
---

# 進捗チェックポイント

開発中の進捗をスナップショットとして記録し、比較・復元します。

## コマンド

### チェックポイント作成

`--create` オプションでスナップショットを作成：

```bash
/dev-core:checkpoint --create "認証機能実装完了"
```

記録される情報：

- タイムスタンプ
- Git コミットハッシュ
- 変更ファイル一覧
- テスト結果
- カバレッジ
- lint/typecheck 状態

### チェックポイント比較

`--compare` オプションで 2 つのチェックポイントを比較：

```bash
/dev-core:checkpoint --compare cp-001 cp-002
```

### チェックポイント一覧

`--list` オプションで一覧表示：

```bash
/dev-core:checkpoint --list
```

### チェックポイント復元

`--restore` オプションで状態を復元（git reset）：

```bash
/dev-core:checkpoint --restore cp-001
```

## 出力形式

### 作成時

```
【チェックポイント作成】

📍 ID: cp-003
📝 説明: 認証機能実装完了
🕐 作成日時: 2024-01-20 15:30:00
🔗 コミット: abc1234

📊 状態:
   ✅ Build: パス
   ✅ Type: パス
   ✅ Lint: パス
   ✅ Test: パス (45/45)
   📈 Coverage: 87%

📁 変更ファイル:
   + src/features/auth/login.ts
   + src/features/auth/login.test.ts
   M src/app/page.tsx
```

### 比較時

```
【チェックポイント比較】

📍 cp-001 → cp-003

📊 メトリクス変化:
   Coverage: 75% → 87% (+12%)
   Tests: 30 → 45 (+15)

📁 ファイル変化:
   新規: 5 ファイル
   変更: 3 ファイル
   削除: 0 ファイル

⏱️ 経過時間: 2 時間 30 分
📝 コミット数: 8
```

## ディレクトリ構造

```
.checkpoints/
├── cp-001.json
├── cp-002.json
└── cp-003.json
```

## チェックポイントファイル形式

```json
{
  "id": "cp-003",
  "description": "認証機能実装完了",
  "timestamp": "2024-01-20T15:30:00Z",
  "commit": "abc1234",
  "state": {
    "build": "pass",
    "type": "pass",
    "lint": "pass",
    "test": { "pass": 45, "fail": 0 },
    "coverage": 87
  },
  "files": {
    "added": ["src/features/auth/login.ts"],
    "modified": ["src/app/page.tsx"],
    "deleted": []
  }
}
```

## 使用例

```bash
# チェックポイント作成
/dev-core:checkpoint --create "Phase 1 完了"

# 一覧表示
/dev-core:checkpoint --list

# 比較
/dev-core:checkpoint --compare cp-001 cp-003

# 最新との比較
/dev-core:checkpoint --compare cp-001 HEAD
```
